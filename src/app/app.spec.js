describe('application', function() {

  beforeEach(module('cz.angular.pushups'));

  it('should have defined module', function() {
    expect(angular.module('cz.angular.pushups')).toBeDefined();
  });

});
